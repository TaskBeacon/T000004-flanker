# Flanker Assets

This task uses PsychoPy text primitives for arrow arrays and does not require
external visual stimulus assets for the canonical Flanker implementation.

Voice files may be generated from config-defined participant instructions during
human runs when voice output is enabled.
